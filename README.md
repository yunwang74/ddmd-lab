
## Analytics
Replace `G-XXXXXXXXXX` with your GA4 Measurement ID in the `<head>` of each page.

## Publications auto-update (ORCID)
Add a repo secret `ORCID_TOKEN` (read-public) and run the Action `Update Publications from ORCID`.
